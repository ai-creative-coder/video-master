---
name: video-master
description: 忠实拆解用户提供的视频；每次先确认标准或深度模式，再按真实镜头拉片，通过可见表演链、首次登场、分层文字/VFX、冲击事件加密取证和证据锁定时间轴，交付完整故事剧本与整合资产、逐镜分析、原片关键帧和 Seedance 2.5 自包含提示词的图文故事板。用于高还原度视频复刻与生成方法反推；不分析爆款原因、传播数据或平台增长。
---

# 视频复刻大师video-master

## 每次调用先确认模式

这是第一步，优先于读取视频、媒体探测、抽帧或启动子 Agent。

- 阅读 [mode-selection.md](references/mode-selection.md)，在普通对话中询问用户选择“标准模式”或“深度分析模式”。
- 用户确认前不要分析视频；确认后继续原请求，不要求重新上传。

## 目标与边界

以用户视频作为分析证据，还原创作方法；最终 Seedance 提示词必须脱离该视频独立可用。视频只用于取证和故事板关键帧，不作为生成时需要再次提交的参考素材。

不要分析爆款原因，不擅自增加人物、剧情、场景或风格。故事板画面只能从原视频提取，不得 AI 重绘。

标准模式和深度模式都只交付：

1. `01-完整故事剧本.md`
2. `02-图文分镜故事板/`

图文故事板必须整合人物/道具/场景资产表、原片关键帧、逐镜动作与表演、摄影方案、光影、声音、分层文字/VFX、每段 Seedance 2.5 整段完整提示词，以及存在时的专项修复提示词。逐镜分镜脚本、独立提示词 Markdown、`prompt-data.json`、专业报告、证据包、manifest 和红队意见只作为内部材料，不重复交付。

## 共享准备

1. 阅读 [rights-and-boundaries.md](references/rights-and-boundaries.md)，确认视频可读取及任务边界。
2. 默认保持原画幅、剧情时长、语言和声音设计。
3. 运行 `scripts/inspect_video.py` 建立媒体信息、自动切镜候选和短促音频瞬态候选。对枪声、撞击、打击、爆炸或异常短促峰值，必须检查其前后约 `0.20` 秒的加密关键帧，逐项确认可见原因、目标、反应和物理结果；自动检测只负责提示，不得自行把声音峰值认定为枪击。
4. 阅读 [story-segments.md](references/story-segments.md)。先建立 `SH001…` 真实镜头，再按完整故事动作划分 `P001…` 剧情片段；不得固定时长切分。
5. 除音频瞬态外，必须检查小体积物体的快速位移、方向突变、离地、接触、抓取、抛接、所有权转移和人物视线追踪。动作跨镜时以同一 `interaction_chain_id`、`interaction_kind` 和 `interaction_object_ids` 锁定被追踪对象；所有权转移至少包含“触发/踢飞→接触/抓取→携带/返回→结果/观察者反应”四类阶段，并在每个接触点前后补帧。上一阶段 `state_out` 必须逐字传给下一阶段 `state_in`；不能把“踢出物体、飞行动物接取、携物返回、人物抬头追视”压缩成一句“动物飞过”。
6. 所有参与者读取同一紧凑证据包。判断标为 `OBSERVED`、`MEASURED`、`INFERRED`、`RECREATION_SPEC`、`UNKNOWN` 或 `USER_PROVIDED`。
7. 摄影设备、镜头型号、焦段和光圈无法从成片证实时，不得冒充原始拍摄参数；应输出可实现相同画面的 `RECREATION_SPEC`。

## 执行模式

- 标准模式：读取 [standard-workflow.md](references/standard-workflow.md) 和 [agent-contracts.md](references/agent-contracts.md)。默认启动视觉综合 Agent；叙事与声音综合 Agent 按内容门控。主 Agent 同时承担 Seedance 提示词编译职责，不另启提示词 Agent。
- 深度模式：读取 [deep-workflow.md](references/deep-workflow.md) 和 [agent-contracts.md](references/agent-contracts.md)。

不要同时执行两种模式，不启动重复或备份 Agent。

两种模式共用同一份 schema 4.0、同一套字段完整度、四段式 `full_prompt`、信息价值与去重规则、缺失字段停止机制和确定性验证。标准模式只减少分析角色与交叉复核，不得缩写、删减或降低最终提示词的可执行信息。

微动作和动态首次登场的责任必须唯一：标准模式由视觉综合 Agent 判定并提交证据；深度模式由导演 Agent 判定，摄影 Agent 只补摄影实现。遇到小物体高速运动、遮挡接触、抓取或所有权转移证据不足时，两种模式都按门槛临时启动剪辑/VFX Agent 做接触前/中/后定向复核，不能让提示词 Agent 猜剧情。

深度模式的红队是轻量 P0/P1 质量门槛：必须在确定性校验通过后运行，只读取最终结构化数据和必要目标片段，不重新拉片、不读取全部专业报告、不输出第二套分析。无 P0/P1 时立即以单行 PASS 结束。

本 Skill 的深度模式固定启用导演、编剧、摄影、美术和提示词五个核心专业 Agent；剪辑/VFX 与声音由主 Agent 按 [agent-contracts.md](references/agent-contracts.md) 的证据门槛决定是否启动。门控结论写入内部 manifest，不能仅凭“看起来简单”跳过专业检查。

## 两项交付

严格按 [analysis-and-output-schema.md](references/analysis-and-output-schema.md) 和 `assets/` 模板组织。

### 完整故事剧本

忠实覆盖视频；没有传统剧情时输出完整视觉动作剧本。人物设定必须给出可用于生成的外貌、服装、体态、表演习惯和关系描述。

### 图文分镜故事板

阅读 [storyboard-rules.md](references/storyboard-rules.md)。故事板首页先展示视频资产表：人物、主要道具、主要场景。每项资产包含原视频代表帧、详细生成提示词、叙事功能和连续性锁定。

其后按完整剧情片段展示逐镜关键帧和逐镜生成设计。每个真实镜头至少一张代表帧；复杂动作、表演、运镜、构图、转场或 VFX 增加节点。对应镜头下方显示完整动作关系、`performance_chain`、动态首次登场、跨镜微事件链、摄影参数、光影、声音和分层文字/VFX；每个剧情片段末尾只显示一次可直接复制的 Seedance 整段完整提示词，不再重复展示单镜头提示词。故事板末尾显示存在的专项修复提示词；没有专项修复项时明确标注无需额外修复提示词。

### 故事板内的 Seedance 2.5 提示词

阅读 [seedance-2.5.md](references/seedance-2.5.md)。提示词必须自包含，可直接复制到新生成任务：

- 不得出现 `@视频1`、`@图片1`、参考映射、要求再次上传视频或依赖播放器上下文。
- 不得使用“入口、出口、承接上一段、忠实复刻上一段硬切”等跨任务指令。
- 必须把所用人物、服装、道具、场景、光影、文字、声音和摄影方法写成具体描述；不能只写资产编号或“保持一致”。
- 每个动作节拍必须明确主体、动作、对象/目标、空间关系、时间节奏和物理反馈；人物或生物另写可见表演链。
- 每个镜头必须包含 `RECREATION_SPEC`：设备名称、镜头、焦段、光圈和运镜；在用户可见 `full_prompt` 中，它不再单列于【氛围质感】，而是追加到所属【时间轴】节点末尾，使剧情位置、人物动作和摄影执行处于同一时间节点。
- 同一剧情片段内的真实切镜直接按时间顺序写明。跨片段一致性通过在每条提示词中重复稳定资产和状态描述实现，不要求模型理解上一轮生成结果。

每条面向用户的 `full_prompt` 固定按以下顺序输出，且【基础设定】与【氛围质感】必须位于【时间轴】之前：

1. 【生成任务】：时长、画幅、帧率、整体类型和剧情目标。
2. 【基础设定】：人物角色、道具与车辆、场景环境、连续性要求、禁止项。
3. 【氛围质感】：先输出贯穿全片的“全片视觉母版”，固定包含核心风格、视觉基调、色彩体系和影调结构；再输出本段光影与色彩、声音设计。摄影方案不在此处另列。全片母版描述稳定的风格规则，本段光影只写剧情、地点、天气或时间造成的局部差异，二者不得重复或互相改调。
4. 【时间轴】：把剧情位置、人物/角色动作与表演、对象状态转移、文字、VFX、镜内剪辑合并为按相对时间排列的自然语言事件；每条采用“时间｜场景位置与谁做什么｜对象/状态变化与必要反馈｜摄影 RECREATION_SPEC”，摄影方案固定放在该时间节点最后，不显示内部字段标签。

同一事实只能有一个权威位置。资产外观不在时间轴重述；全片核心风格、视觉基调、色彩体系和影调结构只在“全片视觉母版”定义；本段光影与色彩只补充母版未覆盖的局部变化；声音不在时间轴全文复述。摄影参数只在其所属时间节点末尾出现，不再在【氛围质感】重复。时间轴只引用已定义对象并补充当时发生的变化。跨剧情片段为了自包含可以重复必要资产，但同一条提示词内部不得用同义改写反复表达同一约束。删除不影响身份、画面、动作、时序、质感或生成边界的句子。

逐镜动作、首次登场、分层文字/VFX 和已确认冲击事件各自提供唯一事实 ID 与简洁 `timeline_fact`。`timeline_events[]` 输入时间和 `source_refs`；构建脚本先验证所有来源事实属于同一真实镜头、事件时间与来源事实正向重叠，再原样编译事实，并在末尾自动追加该镜 `RECREATION_SPEC`。跨镜误挂、边界只相接但不重叠、遗漏、重复、引用不存在、文字改义或摄影方案不属于该镜都会停止构建。禁止把内部字段标签原样串入 `full_prompt`。

逐词口型时码默认不进入时间轴；只有用户明确要求逐词同步，或关键词同时驱动画面文字、CTA、动作/VFX 节点时才保留必要词级时间。`timeline_continuity` 只描述成片可见的镜头连续性，不写“换底片、Alpha 通道、合成节点”等生成模型无法执行的制作过程词。

负责提示词编译的 Agent 只读取导演锁定后的结构化数据：深度模式由 Seedance 生成导演执行，标准模式由主 Agent 执行。若缺少顶层全片视觉母版中的任一字段，或任一片段缺少关键资产、动作关系、摄影、本段光影、文字/VFX、声音、剪辑或连续性字段，必须输出 `NEEDS_INPUT｜片段/镜号｜字段路径｜原因｜责任角色` 并立即停止；不得猜测、重新读取原视频补写或生成部分提示词。主 Agent 定向补齐后再继续。

内部 `prompt-data.json` 是资产表与故事板提示词的唯一真源。使用 `scripts/build_prompt_data.py` 规范数据，`scripts/extract_storyboard_frames.py` 提取原片帧，`scripts/render_storyboard.py` 渲染故事板；内部 JSON 不复制到交付目录。

`shot_prompt` 可保留在 `prompt-data.json` 中作为生成整段提示词和确定性校验的内部字段，但不作为用户可见的第二套提示词展示。面向用户复制使用的唯一主提示词是每个剧情片段的 `full_prompt`。

## 完成门槛

运行 `scripts/validate_outputs.py <交付目录> --prompt-data <内部 prompt-data.json> [--manifest <内部 manifest.json>]`，并确认：

- 两项交付齐全；镜头与剧情时间线连续覆盖全片。
- 人物、道具、场景资产表位于故事板最前方，每项有原视频代表帧和详细提示词。
- 每个真实镜头有代表帧、完整动作关系和摄影复刻参数。
- 全部 Seedance 提示词不依赖参考视频或前一次生成，不含禁止的跨任务措辞。
- 每条整段提示词完整描述本段所需资产、逐镜动作、真实切镜、文字、声音和画面风格。
- 每条整段提示词具有【生成任务】→【基础设定】→【氛围质感】→【时间轴】结构；【氛围质感】依次包含同一份全片视觉母版、本段光影与色彩、声音设计；每个时间轴节点末尾包含且只包含所属真实镜头的摄影 `RECREATION_SPEC`，不存在单列摄影方案或逐字/近似重复长句。
- `full_prompt` 的时间轴使用自然语言事件，不暴露内部字段标签；动作、文字和 VFX 时序不重复，镜头连续性不含不可执行的制作过程词。
- 人物表演链只属于本镜可见人物；跨主体/跨镜微动作以同一交互链 ID、类型、对象 ID 和逐阶段状态保持所有权连续，所有权转移四类阶段齐全；每位人物的首次可见状态有动态登场事件和至少两张本镜证据帧；面屏文字、后期标题和环境 VFX 分类明确。
- 每个确认的短促冲击事件都有前/中/后加密帧和原因→目标→反应→物理结果的因果结论；所有必需逐镜事实通过唯一 ID 恰好进入一次时间轴且未改义。
- 内部 manifest 记录剪辑/VFX 与声音 Agent 的启动或跳过依据；最终交付不得残留 `NEEDS_INPUT`。
- 故事板内的资产、逐镜事实与完整提示词来自同一份内部 `prompt-data.json`，没有严重冲突或未解决 P0/P1；交付目录不得出现独立逐镜脚本、提示词 Markdown 或 `prompt-data.json`。

本 Skill 只生成复刻制作包，不自动提交 Seedance 任务、不承担生成费用，也不保证像素级一致。实际生成需用户另行明确要求。
